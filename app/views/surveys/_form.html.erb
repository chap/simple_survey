<%= form_with(model: survey, local: true) do |form| %>
  <% if survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
        <% survey.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" data-controller="work">
    <label> <input type="checkbox"> Do you work outside the home?</label>
    <!-- <label><input type="radio" value="yes" name="1-work" data-target="work.outsideYes"> yes</label> -->
    <!-- <div class="field" data-controller="">
      <input type="checkbox" value="mask" name="mask"> Do you wear a mask 100% of the time?<br />
      <input type="text" placeholder="optionally share more details on your work here">
    </div> -->
    <!-- <label><input type="radio" value="no" name="1-work" data-target="work.outsideNo"> no</label> -->
  </div>

  <p><input type="checkbox"> Do you have allergies?</p>
  <p><input type="checkbox"> Do you hang out with other people?</p>

  <p><input type="checkbox"> Do you do curbside pickup?</p>

  <p><input type="checkbox"> Do you go out in public places?</p>


  <div class="field" data-controller="occupants">
    <label>How many people do you live with?</label>
    <input type="number" min="0" max="10" value="0" id="occupants-number" data-target="occupants.number" data-action="occupants#updateNumber">
    <input type="range" min="0" max="10" value="0" id="occupants-slider" data-target="occupants.slider" data-action="input->occupants#slide">
  </div>

  <div class="actions">
    <%= form.submit 'Save' %>
  </div>





<% end %>
